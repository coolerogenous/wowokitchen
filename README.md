# 🐶 旺财厨房 (WoWoKitchen)

> **让每一餐都充满期待**  
> *Make every meal full of expectation*

旺财厨房 (WoWoKitchen) 是一个**计算型的厨房管理与社交组局工具**。它旨在解决聚餐场景下的筹备效率问题，将复杂的菜品构成转化为精确的**采购清单**和**成本预算**，并通过“组局”功能让好友轻松参与点菜协作。

---

## ✨ 核心功能 (Features)

### 1. 🥘 资产管理 (Asset Management)
- **食材库**：管理食材单价、规格，是成本计算的基石。
- **菜品库**：通过组合食材定义菜品（BOM逻辑），自动计算单道菜品的预估成本。
- **菜单集**：将多道菜组合成菜单（如“年夜饭”、“减脂餐”），一键生成整桌预算。

### 2. 🧮 智能计算引擎 (Calculation Engine)
- **自动聚合**：智能合并所有菜品中的同类食材消耗量（如：菜A需2个蛋，菜B需3个蛋 -> 自动输出5个蛋）。
- **成本核算**：基于实时单价计算总预估采购成本，拒绝预算超支。
- **清单导出**：生成详细的**采购清单图片**，方便直接去超市对照购买。

### 3. 🤝 社交组局 (Social Party)
- **发起组局**：创建一个“饭局”，分享链接邀请好友加入。
- **无需注册**：好友作为 Guest 可直接进入饭局点菜，无需繁琐注册流程。
- **实时协作**：所有人可见的菜单同步，谁加了什么菜一目了然。
- **状态锁定**：主人可锁定饭局，生成最终定局清单。

### 4. 🔑 密语分享 (Token System)
- **一键克隆**：通过“密语”分享你的拿手菜或菜单。
- **数据导入**：输入密语，即可将他人的菜品配置完整克隆到自己的库中。

---

## 🛠️ 技术栈 (Tech Stack)

本项目采用前后端分离架构，专为高效开发和部署设计。

- **前端 (Frontend)**: 
  - [React](https://reactjs.org/) (Vite)
  - [Zustand](https://github.com/pmndrs/zustand) (状态管理)
  - [Lucide React](https://lucide.dev/) (图标库)
  - CSS Modules / Vanilla CSS (Catno Design System)

- **后端 (Backend)**: 
  - [Node.js](https://nodejs.org/) & [Express](https://expressjs.com/)
  - [SQLite](https://www.sqlite.org/) (开发环境默认数据库，无需安装额外服务)
  - [Sequelize](https://sequelize.org/) (ORM)
  - [Socket.io](https://socket.io/) (实时协作)

---

## 🚀 快速开始 (Getting Started)

### 前置要求 (Prerequisites)
- [Node.js](https://nodejs.org/) (v14+ 推荐)
- [Git](https://git-scm.com/)

### 1. 克隆项目 (Clone Repository)
```bash
git clone https://github.com/your-username/wowokitchen.git
cd wowokitchen
```

### 2. 后端设置 (Backend Setup)
```bash
cd server

# 安装依赖
npm install

# 配置环境变量 (可选，默认已提供 .env.example)
cp .env.example .env

# 初始化数据库 (自动创建 SQLite 文件)
# 开发模式下启动服务 (默认端口 3000)
npm run dev
```

### 3. 前端设置 (Frontend Setup)
新开一个终端窗口：
```bash
cd client

# 安装依赖
npm install

# 启动开发服务器 (默认端口 5173)
npm run dev
```

### 4. 访问应用
打开浏览器访问 `http://localhost:5173` 即可开始使用！

---

## 📂 项目结构 (Project Structure)

```
wowokitchen/
├── client/                 # 前端 React 应用
│   ├── src/
│   │   ├── components/     # 公共组件 (Layout, Toast等)
│   │   ├── pages/          # 页面组件 (Home, Party, Menus...)
│   │   ├── services/       # API 请求封装
│   │   ├── stores/         # Zustand 状态管理
│   │   └── ...
│   └── ...
├── server/                 # 后端 Express 应用
│   ├── config/             # 数据库配置
│   ├── controllers/        # 业务逻辑控制器
│   ├── models/             # Sequelize 数据模型
│   ├── routes/             # API 路由
│   ├── socket/             # Socket.io 逻辑
│   └── app.js              # 入口文件
├── PRD.MD                  # 产品需求文档
└── README.md               # 项目说明
```

---

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request 来改进旺财厨房！

1. Fork 本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的修改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启一个 Pull Request

---

## 📄 许可证 (License)

本项目基于 MIT 许可证开源。详情请参阅 [LICENSE](LICENSE) 文件。
